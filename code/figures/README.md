# Figure scripts

Place the MATLAB scripts used to reproduce the paper's figures in the corresponding `figures01` through `figures10` subdirectories. Appendix figure scripts belong in `a1` and `a2`.

Each script should identify the corresponding figure, required input data, dependencies, expected outputs, and tested MATLAB version.
