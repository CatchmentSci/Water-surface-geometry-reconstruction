# Figure scripts

Place the MATLAB scripts used to reproduce the paper's figures in the corresponding `figure_01` through `figure_10` subdirectories. Appendix figure scripts belong in `figure_a1` and `figure_a2`.

Each script should identify the corresponding figure, required input data, dependencies, expected outputs, and tested MATLAB version.
